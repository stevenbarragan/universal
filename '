extern crate pest;
#[macro_use]
extern crate pest_derive;

use pest::Parser;

#[derive(Parser)]
#[grammar = "ini.pest"]
pub struct UniversalParser;

fn main() {

}

fn parse_string(original :&str) -> Result<> {
    
}

#[cfg(test)]
mod test {
    use supper::*;

    #[test]
    fn variables() {
        assert_eq!("
            ")
    }
}
